import { type FastifyInstance } from 'fastify';
interface DevtoolsBackendOptions {
    port?: number;
    hostname?: string;
}
export declare function broadcastToClients(message: string): void;
export declare function start(opts?: DevtoolsBackendOptions): Promise<{
    server: FastifyInstance;
    port: number;
}>;
export declare function stop(): Promise<void>;
export {};
